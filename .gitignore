/target

# Tool-specific agent pointers (thin @AGENTS.md imports, never committed)
CLAUDE.md
GEMINI.md
.cursor/
.aider*
.windsurfrules
/out

# Personal mesh manifest (may carry private keys) - never commit from the tool repo
/mesh.toml
